{
  "name": "cartdotcom-instagram-reel-brain",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "migrate:local": "wrangler d1 migrations apply REEL_DB --local",
    "migrate:remote": "wrangler d1 migrations apply REEL_DB --remote"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7",
    "@cloudflare/puppeteer": "^1.3.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260801.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.120.0"
  }
}
